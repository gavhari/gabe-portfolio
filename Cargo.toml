[package]
name = "gabe-portfolio"
version = "0.1.0"
edition = "2024"

[dependencies]
yew = { version = "0.23.0", features = ["csr"] }

# Smaller, faster WebAssembly for the deployed build.
[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true